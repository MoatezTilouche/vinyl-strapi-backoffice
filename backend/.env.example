HOST=0.0.0.0
PORT=1337

# Local development placeholders only. Generate fresh random values before any shared/production deployment.
APP_KEYS=CHANGE_ME_APP_KEY_1,CHANGE_ME_APP_KEY_2,CHANGE_ME_APP_KEY_3,CHANGE_ME_APP_KEY_4
API_TOKEN_SALT=CHANGE_ME_API_TOKEN_SALT
ADMIN_JWT_SECRET=CHANGE_ME_ADMIN_JWT_SECRET
TRANSFER_TOKEN_SALT=CHANGE_ME_TRANSFER_TOKEN_SALT
ENCRYPTION_KEY=CHANGE_ME_ENCRYPTION_KEY

DATABASE_CLIENT=postgres
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_NAME=vinyl_backoffice
DATABASE_USERNAME=vinyl
DATABASE_PASSWORD=vinyl_local_dev_only
DATABASE_SSL=false

# Mock is the default and is all that is required for this technical test.
DISCOGS_MODE=mock
DISCOGS_TOKEN=

# Creates a demo tenant on first boot if none exists.
SEED_DEMO=true
