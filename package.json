{
  "name": "vinyl-strapi-backoffice",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c auto \"npm --prefix backend run develop\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run develop",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "npm --prefix backend test",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "verify:structure": "node scripts/verify-project.mjs",
    "verify": "npm run verify:structure && npm test",
    "test:e2e": "npm --prefix backend run test:e2e",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
